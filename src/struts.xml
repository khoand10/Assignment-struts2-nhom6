<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
        "http://struts.apache.org/dtds/struts-2.0.dtd" >

<struts>
    <package name="default" extends="hibernate-default">

        <!-- Interceptors -->
        <interceptors>
            <interceptor name="loginInterceptor" class="com.poly.interceptor.LoginInterceptor"/>
            <interceptor-stack name="loginStack">
                <interceptor-ref name="loginInterceptor"/>
                <interceptor-ref name="defaultStack"/>
            </interceptor-stack>
        </interceptors>

        <!-- Login pages -->
        <action name="login" method="login" class="com.poly.action.LoginAction">
            <result name="input">/login.jsp</result>
        </action>

        <action name="processLogin" class="com.poly.action.LoginAction">
            <result name="success" type="redirectAction">/welcome.jsp</result>
            <result name="error">/error.jsp</result>
            <result name="input">/WEB-INF/pages/login.jsp</result>
        </action>
    </package>
</struts>